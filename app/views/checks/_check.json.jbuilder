json.extract! check, :id, :amount, :checks_number, :check_start, :check_remark_destroy, :check_finish, :reason, :category, :violation, :response_measure, :direction_author, :direction_where, :direction_status, :case_number, :number_of_volumes, :for_office, :created_at, :updated_at
json.url check_url(check, format: :json)
