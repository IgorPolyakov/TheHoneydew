json.extract! check, :id, :amount, :checks_number, :deadline, :reason, :category, :violation, :response_measure, :direction, :term, :case_number, :number_of_volumes, :for_office, :created_at, :updated_at
json.url check_url(check, format: :json)
